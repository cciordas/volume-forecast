[project]
name = "volume-profile"
version = "0.1.0"
description = "Quantitative research project"
requires-python = ">=3.12"
dependencies = [
      "requests",
      "beautifulsoup4",
      "arxiv",
      "pypdf",
      "pyyaml",
      "marker-pdf"
]

